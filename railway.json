{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "alembic upgrade head && uvicorn fastapi_modulo.main:app --host 0.0.0.0 --port ${PORT}",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
